# ingressShim:
# defaultACMEChallengeType: dns01
# defaultACMEDNS01ChallengeProvider: route53
# These configurations have been removed from the newest version on cert manager
  # they will be in ther cert man manifest

#installCRDs has also now been deprecated: use crds.enabled and crds.keep instead.

crds:
 enabled: true

extraArgs:
- --dns01-recursive-nameservers-only
- --dns01-recursive-nameservers="8.8.8.8:53"

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: "${role_arn}"

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: "$"